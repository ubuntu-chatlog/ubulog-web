apiVersion: v1
kind: Service
metadata:
    name: "ubulog-web"
    labels:
        app: "ubulog-web"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "ubulog-web"